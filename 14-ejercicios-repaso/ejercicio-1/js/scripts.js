// 1 - Solicita un nombre, la edad y muestra por consola el mensaje "Hola ____, tienes ____ años y el año que viene tendrás ____ años" Realiza el ejercicio con prompt(mensaje) y haz uso de los template strings

// Declaramos el nombre y la edad

    // let name='TuNombre';
    // let year=2*;

// Mostar por consola -metodo log
    // console.log('Hola '+name +' tienes '+year+' años y el año que viene tendrás '+(year+1)+' años');


// Solicita el nombre y la edad por por prompt

let nombre= prompt('Introdusca su nombre');

let edad =parseInt(prompt('Introdusca su edad'));


// Mostrar por consola metodo log - metodo tempñate string

let incremento= (edad+1);
        console.log(`Hola ${nombre}  tienes ${edad}  años y el año que viene tendrás ${edad+1} años`);
// objeto math
// objeto estatico
    // tenemos que usar su nombre para utilizarlo

// propiedades
    // Math.E   Math.PI

// Metodos
    // Math.abs(x)-> Devuelve el valor absoluto de x.Devuelve
    // Math.ceil(x)-> Devuelve el entero más grande  mayor o igual que el numero.
    // Math.floor(x)-> Devuelve el entero más pequeño menor o igual que un número
    // Math.pow(x)->Devulve la potencia de x elevado a y
    // Math.sqrt(x)->Devuelve la raiz cuadrada de x
    // Math.round(x)->Genera un número pseusoaleatorio entre 0 y 1.
    // Math.random(x)->Devuelve el valor de un numero redondeado el entero más cercano
    // Math.sign(x)-> Devuelve el signo de la X,que indica si x es negativo,positivo p cero

    console.log(Math.E);
    console.log(Math.PI);


    let num= 5.9;
    console.log(Math.abs(num));

    console.log(Math.ceil(num));

    console.log(Math.floor(num));

    console.log(Math.pow(2,8));

    console.log(Math.round(Math.random()*100));

    // console.log(Math.random()*(max-min)+min);
        console.log(Math.round(Math.random()*(100-50)+50));

    console.log(Math.sign(6));

    console.log(Math.sqrt(2));


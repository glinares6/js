// Son estructuras que nos permiten almacenar varios daros y almacenarlas
// Se puede llenar con cualquier tipo de dato en javasript y deben ir separados por comas

    // Se pueden mezclar tipos de daros pero no es recomendable
    // Se declaran con llaves cuadragas o corchetes []
    // Se puede declarar vacioi o con un contenido ya establecido
    // Pueden añadirse o eliminarse elementos en el momentos que queramos.
    

// let array= []  //array vacio
// let numero = [1,3,3,4,5]  //array con contenido incial


let numeros = [1,2,3,4,5];
let palabras =['hola', 'estamos', 'en' ,'youtube'];
let boolean =[true,true,false];

console.log(numeros[0] + numeros[1]);
console.log(`la palabra "${palabras[3]}" tiene ${palabras[3].length} letras`);
console.log(numeros);